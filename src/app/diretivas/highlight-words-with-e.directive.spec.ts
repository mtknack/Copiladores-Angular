import { HighlightWordsWithEDirective } from './highlight-words-with-e.directive';

describe('HighlightWordsWithEDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightWordsWithEDirective();
    expect(directive).toBeTruthy();
  });
});
